[tox]
envlist = py37

[travis]
python =
    3.7: py37

[testenv:flake8]
basepython = python
deps = flake8
commands = flake8 rnalysis

[testenv]
setenv =
    PYTHONPATH = {toxinidir}
deps =
    -r{toxinidir}/requirements_dev.txt
; If you want to make tox run the tests with the same versions, create a
; requirements.txt with the pinned versions and uncomment the following line:
;     -r{toxinidir}/requirements.txt
changedir = tests
commands =
    pip install -U pip
;    py.test --basetemp={envtmpdir}
;    pytest {posargs}
    codecov
    pytest --basetemp="{envtmpdir}" --cov-report xml --cov={toxinidir}/rnalysis {toxinidir}/tests/ {posargs}
